투자 일지 서비스 개발 현황

작성일: 2026-09-23

1. 프로젝트 목표

주식 투자 과정에서 발생하는 시장 변화, 뉴스, 투자 판단, 투자 가설의 변화를 기록하고 나중에 복기할 수 있는 개인 투자 일지 서비스.

핵심 구조:

사실 → 해석 → 판단 → 투자 가설 변화 → 실제 결과 → 복기

단순한 주가/뉴스 저장 서비스가 아니라 시간이 지나면서 자신의 투자 판단이 어떻게 변화했는지를 확인하는 것을 목표로 한다.

2. 현재 기술 스택
Frontend

Vite

Vanilla JavaScript

HTML

CSS

ES Modules

Backend / Database

Supabase

PostgreSQL

Supabase Auth 예정

Row Level Security(RLS)

Deployment

Vercel 예정

3. 프로젝트 위치
C:\Users\pc501\Downloads\--main\--main


프로젝트명:

investment-journal

4. 현재 프로젝트 구조
investment-journal/
│
├── index.html
├── style.css
├── README.md
│
├── components/
│   ├── Header.js
│   ├── CardHeader.js
│   ├── MarketCard.js
│   ├── NewsCard.js
│   ├── PortfolioCard.js
│   ├── TradesCard.js
│   ├── MemoCard.js
│   └── FocusCard.js
│
├── js/
│   ├── app.js
│   │
│   ├── api/
│   │   └── marketApi.js
│   │
│   ├── services/
│   │   └── marketService.js
│   │
│   └── data/
│       └── dashboard.js
│
├── src/
│   └── supabase.js
│
├── .env
├── package.json
└── package-lock.json

5. 애플리케이션 실행 구조

React가 아닌 Vanilla JavaScript 기반이다.

현재 실행 흐름:

index.html
    ↓
js/app.js
    ↓
components/*.js
    ↓
화면 렌더링


index.html에는 다음과 같이 되어 있다.

<script type="module" src="/js/app.js"></script>


따라서 ES Module 방식으로 import/export를 사용한다.

6. npm 상태

Supabase JavaScript Client 설치 완료.

설치 명령:

npm install @supabase/supabase-js


package.json에:

@supabase/supabase-js


가 설치되어 있다.

7. 개발 명령어
개발 서버
npm run dev


개발 중에는 파일을 저장하면 Vite가 자동으로 변경사항을 반영한다.

매번 npm run build를 실행할 필요가 없다.

Production Build
npm run build

Production Build 결과 확인
npm run preview

8. 환경변수

프로젝트 루트에 .env가 존재한다.

현재 중요한 설정:

VITE_SUPABASE_URL=https://jyoftvzfegcofqvnitke.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=기존_Publishable_Key


중요:

VITE_SUPABASE_URL에는 /rest/v1을 붙이지 않는다.

잘못된 예:

https://jyoftvzfegcofqvnitke.supabase.co/rest/v1


정상:

https://jyoftvzfegcofqvnitke.supabase.co


이 문제로 처음에는 다음 오류가 발생했다.

PGRST125
Invalid path specified in request URL


원인은:

/rest/v1/rest/v1/journal_entries


처럼 rest/v1이 중복된 것이었다.

현재 이 문제는 해결되었다.

9. Supabase Client

파일:

src/supabase.js


현재 코드:

import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseKey = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY

export const supabase = createClient(
  supabaseUrl,
  supabaseKey
)

10. Supabase 연결 상태
현재 상태: 성공

브라우저 Console에서:

[Supabase] 연결 성공: []


확인됨.

따라서 현재 다음 연결은 정상이다.

Vite
 ↓
js/app.js
 ↓
src/supabase.js
 ↓
Supabase
 ↓
PostgreSQL


[]는 현재 journal_entries에 데이터가 없기 때문에 나오는 정상적인 결과다.

즉 Supabase 연결 자체는 완료된 상태다.

11. 현재 Supabase 연결 테스트

js/app.js에서 다음과 같이 테스트한다.

const { data, error } = await supabase
  .from("journal_entries")
  .select("id")
  .limit(1);


성공하면:

[Supabase] 연결 성공: []


을 출력한다.

현재 이 부분은 정상 작동한다.

12. 현재 DB 구조

투자 일지 서비스를 위해 다음 테이블 구조를 계획했다.

profiles
journal_entries
market_indicators
stocks
investment_theses
journal_stocks
news
investment_decisions
tomorrow_checks
thesis_reviews


주요 목적:

journal_entries
→ 하루의 투자 일지

market_indicators
→ 시장 지표

stocks
→ 관심/보유 종목

investment_theses
→ 투자 가설

journal_stocks
→ 해당 날짜의 종목 기록

news
→ 핵심 뉴스 및 사실/해석/판단

investment_decisions
→ 매수/보유/매도 등의 판단

tomorrow_checks
→ 다음 날 확인할 사항

thesis_reviews
→ 투자 가설의 변화 기록

13. 개인 데이터 보안

Supabase RLS를 사용하여 개인 기록을 보호한다.

목표:

로그인한 사용자
    ↓
자신의 데이터만
조회 / 생성 / 수정 / 삭제


사용자 식별:

auth.uid()


예:

사용자 A
→ A의 투자일지만 접근 가능

사용자 B
→ B의 투자일지만 접근 가능


다른 사용자의 투자 기록은 접근할 수 없도록 한다.

14. 현재 기존 대시보드 상태

기존 대시보드 렌더링 기능은 유지되고 있다.

js/app.js는:

dashboardData
↓
Header
↓
MarketCard
↓
NewsCard
↓
PortfolioCard
↓
TradesCard
↓
MemoCard
↓
FocusCard


를 이용해서 화면을 구성한다.

시장 데이터는:

js/api/marketApi.js


를 통해 가져오려고 한다.

15. 현재 발생하는 유일한 주요 오류 — KRX CORS

현재 Console:

[Market API] 요청 시작


이후:

Access to fetch at
'https://data-dbg.krx.co.kr/svc/apis'
from origin
'http://localhost:5173'
has been blocked by CORS policy


그리고:

GET https://data-dbg.krx.co.kr/svc/apis
net::ERR_FAILED


발생.

결과:

[App] 시장 데이터 조회 실패
TypeError: Failed to fetch

[App] fallback 데이터 사용

16. KRX 오류의 의미

이 오류는 Supabase 오류가 아니다.

현재 구조:

브라우저
    ↓
http://localhost:5173
    ↓
KRX API
    ↓
CORS 차단


KRX API가 현재 브라우저에서 직접 호출되는 요청을 허용하지 않아 발생한다.

따라서:

Supabase
→ 정상

KRX API
→ 브라우저 직접 호출 불가


상태다.

17. KRX 오류 때문에 앱이 완전히 멈추지는 않는다

현재 loadMarketData()에 fallback이 구현되어 있다.

KRX API 요청
    ↓
실패
    ↓
dashboardData.market
    ↓
기존 fallback 시장 데이터 표시


그래서 현재 대시보드 화면 자체는 계속 렌더링된다.

Console에도:

[App] fallback 데이터 사용


이 출력된다.

18. KRX API 문제의 향후 해결 방향

KRX 데이터를 실제로 사용하려면 브라우저에서 직접 KRX를 호출하지 않는 구조로 변경해야 한다.

현재:

브라우저
 ↓
KRX


가 아니라 향후:

브라우저
 ↓
우리 서버/API
 ↓
KRX


형태로 변경하는 것이 필요하다.

Vercel 배포 시에는 서버 측 API 기능 등을 이용해 KRX 요청을 중계하는 방식을 검토한다.

이 문제는 현재 Supabase 연결과 별개이므로 지금 당장 해결할 필요는 없다.

19. 현재 완료된 것
✓ Vite 프로젝트 확인
✓ Vanilla JavaScript 구조 확인
✓ 기존 대시보드 코드 확인
✓ npm 환경 확인
✓ @supabase/supabase-js 설치
✓ Supabase DB 구조 설계
✓ .env 생성
✓ VITE_SUPABASE_URL 설정 수정
✓ VITE_SUPABASE_PUBLISHABLE_KEY 설정
✓ src/supabase.js 생성
✓ js/app.js에서 Supabase import
✓ Supabase 연결 테스트 구현
✓ Supabase 실제 연결 성공 확인
✓ journal_entries 조회 테스트 성공
✓ 기존 대시보드 fallback 구조 유지

20. 아직 하지 않은 것
□ Supabase Auth 회원가입
□ Supabase Auth 로그인
□ 로그아웃
□ 현재 로그인 사용자 확인
□ profiles 사용자 프로필 연결
□ journal_entries 실제 데이터 저장
□ 투자 일지 CRUD
□ 종목 CRUD
□ 뉴스 기록
□ 투자 가설 기록
□ 투자 판단 기록
□ 내일 확인할 것
□ 주간 복기
□ 월간 복기
□ KRX API CORS 해결
□ Vercel 배포

21. 다음 작업 순서

현재는 Supabase 연결이 성공했으므로 다음 작업은 인증 기능이다.

권장 순서:

현재
↓
Supabase 연결 성공
↓
1. 회원가입 화면
↓
2. 로그인 화면
↓
3. 로그아웃
↓
4. 현재 로그인 사용자 확인
↓
5. RLS 동작 확인
↓
6. journal_entries 저장
↓
7. 내 일지 불러오기
↓
8. 일지 수정
↓
9. 일지 삭제


그 다음:

종목 관리
↓
투자 가설
↓
뉴스
↓
투자 판단
↓
내일 확인
↓
주간 복기
↓
월간 복기


마지막:

KRX API 서버 측 연동
↓
npm run build
↓
GitHub
↓
Vercel
↓
Vercel 환경변수 설정
↓
배포

22. 현재 가장 중요한 상태

현재 개발의 핵심은 다음과 같다.

                    현재 상태

Vite
  │
  ├── 기존 대시보드 ───────── 정상
  │
  ├── Supabase ────────────── 정상
  │
  └── KRX API ─────────────── CORS 문제
                              │
                              └── fallback 사용 중


따라서 Supabase 연결 작업은 다음 단계로 진행해도 된다.

23. 다음 개발자가 바로 해야 할 작업

다음 작업은:

Supabase Auth 회원가입 / 로그인 구현

이다.

목표:

회원가입
↓
Supabase Auth에 사용자 생성
↓
로그인
↓
현재 user 확인
↓
로그인한 사용자만 자신의 투자 일지 접근


그 후 실제로:

journal_entries


에 첫 번째 투자 일지를 저장하는 것까지 테스트한다.

24. 현재 상태 한 줄 요약

Vite + Vanilla JavaScript 기반 투자 일지 서비스에서 Supabase Client 설치, 환경변수 설정, Supabase Client 연결 및 PostgreSQL 조회 테스트까지 성공했으며, 현재 남은 콘솔 오류는 KRX API의 브라우저 CORS 문제이고 기존 fallback으로 대시보드는 정상적으로 동작하는 상태이다. 다음 작업은 Supabase Auth 회원가입/로그인 구현이다.